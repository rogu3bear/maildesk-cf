[package]
name = "maildesk-router"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Rust policy core for Cloudflare-edge mail routing."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lints]
workspace = true
