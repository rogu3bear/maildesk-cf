{
  "name": "maildesk-cf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run typecheck",
    "preflight": "bun run scripts/preflight.ts",
    "preflight:template": "bun run scripts/preflight.ts --mode template",
    "preflight:production": "bun run scripts/preflight.ts --mode production",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260519.0",
    "@types/node": "^25.9.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.50.0"
  }
}
