# maildesk-ui

Placeholder for the Leptos-compatible operator UI.

This app should eventually provide:

- thread list;
- thread detail;
- reply composer;
- allowed identity selector;
- audit event view;
- deployment/config health view.

Keep the UI compatible with `leptos-cf` conventions, but do not make
`leptos-cf` a hard dependency without an explicit design decision.
